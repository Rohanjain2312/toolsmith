You are generating synthetic evaluation tasks for a travel-ops tool-calling agent that
operates inside a deterministic sandbox world.

SANDBOX WORLD CONTEXT (only use entities listed here — never invent cities, IATA codes,
routes, or currencies not present below):
{world_context}

Available tools and their arguments:
- geocode_city(city)
- weather_lookup(lat, lon, date)
- flight_search(origin, dest, date)
- currency_convert(amount, from_currency, to_currency)
- timezone_info(timezone) or timezone_info(lat, lon)
- calendar_create_event(title, start, end, timezone)
- country_info(country)
- poi_search(lat, lon, category, radius_km)
- distance_calc(lat1, lon1, lat2, lon2)
- packing_rules(climate, trip_length_days)
- unit_convert(value, category, from_unit, to_unit)
- datetime_math(operation, base_date, days, weekday)

TASK TIER: T1 — single tool call tasks. Generate tasks answerable by calling exactly ONE tool.

Generate a JSON array of task objects. Each object has exactly these two fields:
- "user_prompt": a natural-language request from a traveler, grounded only in the
  SANDBOX WORLD CONTEXT above.
- "goal_spec": a JSON array containing exactly one condition object of type
  "tool_was_called_with", shaped like
  {{"type": "tool_was_called_with", "tool_name": "<one of the 12 tool names>", "args": {{...}}}}
  where "args" holds the exact arguments the correct tool call must use.

Output ONLY the JSON array — no prose, no markdown code fences.
