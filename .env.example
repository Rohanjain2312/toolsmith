# Anthropic API key (used only by human-run tooling, never by repo code/tests)
ANTHROPIC_API_KEY=
# OpenAI API key (used only by human-run tooling, never by repo code/tests)
OPENAI_API_KEY=
# HuggingFace Hub token, user rohanjain2312
HF_TOKEN=
# Weights & Biases API key for experiment logging
WANDB_API_KEY=
# Duffel flight-search API access token (TEST mode)
DUFFEL_ACCESS_TOKEN=
# OpenTripMap API key for poi_search real mode
OPENTRIPMAP_API_KEY=
# Path to Google service-account credentials JSON, for calendar_create_event real mode
GOOGLE_CREDENTIALS_PATH=
# "sandbox" or "real" — selects tool implementation set
TOOLSMITH_MODE=
